apiVersion: v1
kind: Service
metadata:
  name: {{ include "userliza.fullname" . }}
  labels:
    {{- include "userliza.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "userliza.selectorLabels" . | nindent 4 }}
