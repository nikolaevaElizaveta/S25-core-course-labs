apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "userliza.fullname" . }}-test-connection"
  labels:
    {{- include "userliza.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "userliza.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
